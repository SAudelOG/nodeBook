<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Meadowlark Travel</title>
	<link rel="stylesheet" href="/vendor/bootstrap-3.3.4-dist/css/bootstrap.min.css">
	{{#if showTests}}
		<link rel="stylesheet" href="/vendor/mocha.css">
	{{/if}}
	{{{_sections.head}}}
</head>
<body>
	{{#if flash}}
		<div class="alert alert-dismissible alert-{{flash.type}}">
			<button tpye="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
			<strong>{{flash.intro}}</strong>{{flash.message}}
		</div>
	{{/if}}
	<header><img src="/img/logo.png" alt="Meadowlark Travel Logo"></header>
	<div class="container">
		{{{body}}}
	</div>
	<script src="//code.jquery.com/jquery-2.0.2.min.js"></script>
	<script src="/vendor/bootstrap-3.3.4-dist/js/bootstrap.js"></script>
	{{{_sections.jquery}}}
		{{#if showTests}}
			<div id="mocha"></div>
			<script src="/vendor/mocha.js"></script>
			<script src="/vendor/chai.js"></script>
			<script>
					mocha.ui('tdd');
					var assert = chai.assert;
			</script>
			<script src="/qa/tests-global.js"></script>
			{{#if pageTestScript}}
					<script src="{{pageTestScript}}"></script>
			{{/if}}
			<script>mocha.run();</script>
		{{/if}}
</body>
</html>